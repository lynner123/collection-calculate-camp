'use strict';

function spilt_to_zero(number, interval) {
//  let result = [];
//  var sub = number;
//  
//  while(sub > 0){
//            result.push(sub);
//            sub = Math.round(sub - interval);
//  }
//  return result;
}

module.exports = spilt_to_zero;
